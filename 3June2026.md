# Day 5 Internship Documentation: EuroSAT Image Classification Using TensorFlow and PyTorch

**Date:** 3 June 2026  
**Intern:** Aditya Sharma  
**Domain:** Artificial Intelligence & Machine Learning  
**Task:** Perform EuroSAT Satellite Image Classification Using TensorFlow and PyTorch

---

# Objective

The objective of this task was to perform multi-class satellite image classification using the EuroSAT dataset and implement the solution using both TensorFlow and PyTorch frameworks. The task aimed to understand image preprocessing, dataset handling, convolutional neural networks (CNNs), model training, evaluation, and comparison between the two popular deep learning frameworks.

---

# Introduction

Image classification is one of the most important applications of Computer Vision and Deep Learning. Satellite image classification is widely used in land cover analysis, urban planning, agriculture monitoring, environmental studies, and remote sensing applications.

The EuroSAT dataset contains satellite images captured by the Sentinel-2 satellite and consists of multiple terrain and land-use categories. The project involved building CNN-based image classification systems in TensorFlow and PyTorch and comparing their implementation workflows and performance.

---

# Dataset Information

## Dataset Used

EuroSAT Dataset

## Dataset Characteristics

- Total Images: 2,750
- Number of Classes: 10
- RGB Images
- Satellite Imagery
- Multi-Class Classification Problem

## Classes

- AnnualCrop
- Forest
- HerbaceousVegetation
- Highway
- Industrial
- Pasture
- PermanentCrop
- Residential
- River
- SeaLake

---

# Technologies Used

## TensorFlow Implementation

- Python
- TensorFlow
- Keras
- NumPy
- Matplotlib

## PyTorch Implementation

- Python
- PyTorch
- TorchVision
- NumPy
- Matplotlib

---

# Part A: EuroSAT Image Classification Using TensorFlow

## Dataset Loading

The dataset was loaded using TensorFlow's image dataset API.

```python
train_ds = tf.keras.utils.image_dataset_from_directory(
    DATASET_PATH,
    validation_split=0.2,
    subset="training",
    seed=42,
    image_size=(64,64),
    batch_size=32
)

val_ds = tf.keras.utils.image_dataset_from_directory(
    DATASET_PATH,
    validation_split=0.2,
    subset="validation",
    seed=42,
    image_size=(64,64),
    batch_size=32
)
```

---

## Data Preprocessing

Images were normalized before training.

```python
normalization_layer = tf.keras.layers.Rescaling(1./255)

train_ds = train_ds.map(
    lambda x, y: (normalization_layer(x), y)
)

val_ds = val_ds.map(
    lambda x, y: (normalization_layer(x), y)
)
```

---

## CNN Architecture

```python
model = tf.keras.Sequential([

    tf.keras.layers.Input(shape=(64,64,3)),

    tf.keras.layers.Conv2D(
        32,(3,3),
        activation='relu',
        padding='same'
    ),

    tf.keras.layers.MaxPooling2D(),

    tf.keras.layers.Conv2D(
        64,(3,3),
        activation='relu',
        padding='same'
    ),

    tf.keras.layers.MaxPooling2D(),

    tf.keras.layers.Conv2D(
        128,(3,3),
        activation='relu',
        padding='same'
    ),

    tf.keras.layers.MaxPooling2D(),

    tf.keras.layers.Flatten(),

    tf.keras.layers.Dense(
        256,
        activation='relu'
    ),

    tf.keras.layers.Dropout(0.4),

    tf.keras.layers.Dense(
        10,
        activation='softmax'
    )
])
```

---

## Model Compilation

```python
model.compile(
    optimizer='adam',
    loss='sparse_categorical_crossentropy',
    metrics=['accuracy']
)
```

---

## Model Training

```python
history = model.fit(
    train_ds,
    validation_data=val_ds,
    epochs=15
)
```

---

## Concepts Learned in TensorFlow

### Image Dataset Loading

TensorFlow provides automated dataset loading directly from image folders.

### Data Pipelines

The tf.data pipeline efficiently handles preprocessing, batching, and prefetching.

### CNN Architecture

Multiple convolutional layers help extract hierarchical image features.

### Softmax Classification

Softmax was used for multi-class classification across ten classes.

---

## TensorFlow Results

- Successfully loaded and processed the EuroSAT dataset.
- Built a CNN architecture for image classification.
- Generated predictions for unseen satellite images.
- Visualized training and validation accuracy.

Expected Accuracy:

**85% – 95%**

---

# Part B: EuroSAT Image Classification Using PyTorch

## Dataset Loading

PyTorch's ImageFolder API was used to load images.

```python
dataset = datasets.ImageFolder(
    root=DATASET_PATH,
    transform=transform
)
```

---

## Data Transformations

```python
transform = transforms.Compose([

    transforms.Resize((64,64)),

    transforms.RandomHorizontalFlip(),

    transforms.RandomRotation(10),

    transforms.ToTensor(),

    transforms.Normalize(
        mean=[0.485,0.456,0.406],
        std=[0.229,0.224,0.225]
    )
])
```

---

## CNN Architecture

```python
class EuroSATCNN(nn.Module):

    def __init__(self):

        super().__init__()

        self.conv_layers = nn.Sequential(

            nn.Conv2d(
                3,32,3,padding=1
            ),

            nn.ReLU(),

            nn.MaxPool2d(2),

            nn.Conv2d(
                32,64,3,padding=1
            ),

            nn.ReLU(),

            nn.MaxPool2d(2),

            nn.Conv2d(
                64,128,3,padding=1
            ),

            nn.ReLU(),

            nn.MaxPool2d(2)
        )
```

---

## Loss Function and Optimizer

```python
criterion = nn.CrossEntropyLoss()

optimizer = optim.Adam(
    model.parameters(),
    lr=0.001
)
```

---

## Training Loop

```python
for epoch in range(epochs):

    model.train()

    for images, labels in train_loader:

        images = images.to(device)
        labels = labels.to(device)

        optimizer.zero_grad()

        outputs = model(images)

        loss = criterion(
            outputs,
            labels
        )

        loss.backward()

        optimizer.step()
```

---

## Model Evaluation

```python
model.eval()

with torch.no_grad():

    for images, labels in val_loader:

        outputs = model(images)

        _, predicted = torch.max(
            outputs,
            1
        )
```

---

## Concepts Learned in PyTorch

### ImageFolder Dataset

ImageFolder automatically loads images based on folder structure.

### DataLoader

DataLoader performs batching and shuffling of training data.

### Custom Training Loops

PyTorch allows full control over forward propagation, backpropagation, and optimization.

### Model Saving

The best-performing model can be stored using checkpointing techniques.

---

## PyTorch Results

- Successfully loaded and transformed satellite images.
- Implemented a custom CNN architecture.
- Trained and validated the model using custom loops.
- Visualized classification performance.

Expected Accuracy:

**85% – 95%**

---

# TensorFlow vs PyTorch Comparison

| Feature | TensorFlow | PyTorch |
|----------|------------|----------|
| Dataset Loading | image_dataset_from_directory | ImageFolder |
| Training Method | model.fit() | Custom Loop |
| Learning Curve | Easier | Moderate |
| Flexibility | Moderate | High |
| Deployment Support | Excellent | Good |
| Debugging | Moderate | Easier |

---

# Learning Outcomes

Through this task, the following concepts were learned:

- Satellite Image Classification
- EuroSAT Dataset Handling
- TensorFlow Data Pipelines
- PyTorch Data Loaders
- Convolutional Neural Networks
- Multi-Class Classification
- Image Preprocessing
- Data Augmentation
- Model Training and Validation
- Performance Visualization
- Framework Comparison
- Deep Learning Workflow

---

# Conclusion

The EuroSAT image classification project was successfully implemented using both TensorFlow and PyTorch frameworks. The project provided hands-on experience in working with satellite image datasets, designing convolutional neural networks, and performing multi-class image classification. Implementing the same problem using two different deep learning frameworks helped in understanding their workflows, strengths, and practical applications. This task significantly improved knowledge of computer vision, deep learning architectures, and real-world image classification systems.

---
**End of Day 4 Documentation**
