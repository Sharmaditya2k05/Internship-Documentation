# Day 16 Internship Documentation: Flask Web Application for Iris Flower Prediction

**Date:** 22 June 2026
**Intern:** Aditya Sharma
**Domain:** Artificial Intelligence & Machine Learning
**Task:** Development of a Flask Web Application for Iris Flower Prediction

---

# Objective

The objective of this task was to develop a web-based machine learning application using the Flask framework. The application allows users to enter the measurements of an Iris flower through a web interface and predicts the corresponding flower species using a trained Random Forest classification model.

---

# Introduction

Machine learning models are commonly trained in Python environments but require deployment to be useful in real-world applications. Flask is a lightweight Python web framework that enables developers to deploy machine learning models as interactive web applications.

In this task, a Flask web application was developed that accepts user input through an HTML form, processes the input using a trained Random Forest classifier, and displays the predicted Iris flower species along with the prediction confidence.

---

# Dataset Information

## Dataset Used

**Iris Flower Dataset**

### Dataset Characteristics

* Total Samples: 150
* Features: 4
* Classes: 3

### Input Features

* Sepal Length
* Sepal Width
* Petal Length
* Petal Width

### Output Classes

* Setosa
* Versicolor
* Virginica

---

# Technologies Used

* Python
* Flask
* HTML
* CSS
* NumPy
* Scikit-Learn
* Jupyter Notebook / VS Code

---

# Project Structure

```text
Flask_Iris/

│── app.py

│── templates/

│      └── index.html

└── requirements.txt
```

---

# Workflow

```text
User

↓

Open Flask Web Application

↓

Enter Flower Measurements

↓

Click Predict

↓

Flask Receives Form Data

↓

Random Forest Model

↓

Prediction Generated

↓

Prediction Displayed on Web Page
```

---

# Input Parameters

The user provides the following measurements through the web form:

* Sepal Length
* Sepal Width
* Petal Length
* Petal Width

---

# Output

The application displays:

* Predicted Iris Species
* Prediction Confidence (%)

Example Output

```text
Prediction : Setosa

Confidence : 100%
```

---

# Implementation Steps

1. Load the Iris dataset.
2. Train a Random Forest classifier.
3. Create the Flask application.
4. Design an HTML form for user input.
5. Receive form data using a POST request.
6. Convert the input into a NumPy array.
7. Predict the flower species using the trained model.
8. Display the prediction and confidence on the webpage.

---

# Source Code

## app.py

```python
from flask import Flask, render_template, request
from sklearn.datasets import load_iris
from sklearn.ensemble import RandomForestClassifier
import numpy as np

app = Flask(__name__)

iris = load_iris()

X = iris.data
y = iris.target

model = RandomForestClassifier(random_state=42)
model.fit(X, y)

classes = [
    "Setosa",
    "Versicolor",
    "Virginica"
]

@app.route("/")
def home():
    return render_template("index.html")

@app.route("/predict", methods=["POST"])
def predict():

    sepal_length = float(request.form["sepal_length"])
    sepal_width = float(request.form["sepal_width"])
    petal_length = float(request.form["petal_length"])
    petal_width = float(request.form["petal_width"])

    features = np.array([[
        sepal_length,
        sepal_width,
        petal_length,
        petal_width
    ]])

    prediction = model.predict(features)[0]
    probability = model.predict_proba(features)[0]

    result = classes[prediction]
    confidence = round(max(probability) * 100, 2)

    return render_template(
        "index.html",
        prediction=result,
        confidence=confidence
    )

if __name__ == "__main__":
    app.run(debug=True)
```

---

## templates/index.html

```html
<!DOCTYPE html>
<html>

<head>

<title>Iris Flower Prediction</title>

<style>

body{
font-family:Arial;
background:#f2f2f2;
text-align:center;
margin-top:40px;
}

.container{
width:400px;
margin:auto;
background:white;
padding:20px;
border-radius:10px;
box-shadow:0px 0px 10px gray;
}

input{
width:90%;
padding:10px;
margin:10px;
font-size:16px;
}

button{
padding:10px 20px;
font-size:16px;
background:#4CAF50;
color:white;
border:none;
cursor:pointer;
}

h2{
color:green;
}

</style>

</head>

<body>

<div class="container">

<h1>Iris Flower Prediction</h1>

<form action="/predict" method="post">

<input
type="number"
step="any"
name="sepal_length"
placeholder="Sepal Length"
required>

<input
type="number"
step="any"
name="sepal_width"
placeholder="Sepal Width"
required>

<input
type="number"
step="any"
name="petal_length"
placeholder="Petal Length"
required>

<input
type="number"
step="any"
name="petal_width"
placeholder="Petal Width"
required>

<button type="submit">
Predict
</button>

</form>

{% if prediction %}

<h2>Prediction : {{ prediction }}</h2>

<h3>Confidence : {{ confidence }}%</h3>

{% endif %}

</div>

</body>

</html>
```

---

# Advantages

* Lightweight and easy to develop.
* Interactive web interface.
* Real-time prediction.
* Easy integration with machine learning models.
* Suitable for deployment demonstrations.

---

# Applications

* AI-powered Web Applications
* Educational Projects
* Machine Learning Deployment
* Healthcare Prediction Systems
* Intelligent Decision Support Systems

---

# Learning Outcomes

* Flask Framework
* Web Application Development
* HTML Form Handling
* GET and POST Requests
* Random Forest Classification
* Machine Learning Deployment
* Frontend–Backend Integration
* Real-Time Prediction

---

# Conclusion

A complete Flask web application was successfully developed for Iris Flower Classification. The project integrated a trained Random Forest model with an HTML interface, enabling users to enter flower measurements and obtain real-time predictions. This task enhanced practical knowledge of Flask, machine learning deployment, web development, and integrating AI models into interactive applications.

---
