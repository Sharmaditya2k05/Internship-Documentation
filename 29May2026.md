# Day 2 Internship Documentation: Enhancing CNN Performance Using Additional Convolutional Layers and Dropout

**Date:** 30 May 2026
**Intern:** Aditya Sharma
**Domain:** Artificial Intelligence & Machine Learning
**Task:** Improve CNN Performance by Adding Multiple Convolution Layers and Dropout Regularization

---

# Objective

The objective of this task was to improve the performance of a Convolutional Neural Network (CNN) by introducing additional convolutional layers and dropout regularization. The experiment aimed to understand how deeper architectures capture more complex image features and how dropout helps prevent overfitting.

---

# Introduction

In Day 1, a basic CNN was implemented using a single convolutional layer. While the model achieved reasonable accuracy, deeper CNN architectures generally perform better because they can learn hierarchical image features.

A deeper CNN learns:

* Low-level features (edges and corners)
* Mid-level features (textures and shapes)
* High-level features (object parts)

Additionally, Dropout Regularization was introduced to reduce overfitting and improve the model's ability to generalize to unseen data.

---

# Dataset Information

Dataset Used: Fashion MNIST

Dataset Characteristics:

* 60,000 Training Images
* 10,000 Testing Images
* 28 × 28 Grayscale Images
* 10 Clothing Categories

---

# Technologies Used

* Python
* TensorFlow
* Keras
* NumPy

---

# Code Implementation

```python
import tensorflow as tf
import matplotlib.pyplot as plt

from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import (
    Conv2D,
    MaxPooling2D,
    Dense,
    Flatten,
    Dropout,
    BatchNormalization,
    Input
)



(X_train, y_train), (X_test, y_test) = tf.keras.datasets.fashion_mnist.load_data()



class_names = [
    "T-Shirt",
    "Trouser",
    "Pullover",
    "Dress",
    "Coat",
    "Sandal",
    "Shirt",
    "Sneaker",
    "Bag",
    "Ankle Boot"
]

plt.figure(figsize=(8,8))

for i in range(9):
    plt.subplot(3,3,i+1)
    plt.imshow(X_train[i], cmap='gray')
    plt.title(class_names[y_train[i]])
    plt.axis('off')

plt.tight_layout()
plt.show()



X_train = X_train.astype("float32") / 255.0
X_test = X_test.astype("float32") / 255.0



X_train = X_train.reshape(-1,28,28,1)
X_test = X_test.reshape(-1,28,28,1)



model = Sequential([

    Input(shape=(28,28,1)),

    # Block 1
    Conv2D(32,(3,3),padding='same',activation='relu'),
    BatchNormalization(),

    Conv2D(32,(3,3),padding='same',activation='relu'),
    MaxPooling2D((2,2)),

    # Block 2
    Conv2D(64,(3,3),padding='same',activation='relu'),
    BatchNormalization(),

    Conv2D(64,(3,3),padding='same',activation='relu'),
    MaxPooling2D((2,2)),

    # Classifier
    Flatten(),

    Dense(128,activation='relu'),
    Dropout(0.4),

    Dense(10,activation='softmax')
])


model.compile(
    optimizer='adam',
    loss='sparse_categorical_crossentropy',
    metrics=['accuracy']
)


early_stop = tf.keras.callbacks.EarlyStopping(
    monitor='val_accuracy',
    patience=4,
    restore_best_weights=True
)

reduce_lr = tf.keras.callbacks.ReduceLROnPlateau(
    monitor='val_loss',
    factor=0.5,
    patience=2,
    verbose=1
)


history = model.fit(
    X_train,
    y_train,
    epochs=20,
    batch_size=64,
    validation_split=0.1,
    callbacks=[early_stop, reduce_lr]
)



loss, accuracy = model.evaluate(X_test, y_test)

print(f"\nTest Accuracy: {accuracy*100:.2f}%")



predictions = model.predict(X_test)

for i in range(5):

    predicted = class_names[
        tf.argmax(predictions[i]).numpy()
    ]

    actual = class_names[
        y_test[i]
    ]

    print(f"\nPredicted: {predicted}")
    print(f"Actual   : {actual}")


plt.figure(figsize=(10,5))

plt.plot(history.history['accuracy'], label='Training Accuracy')
plt.plot(history.history['val_accuracy'], label='Validation Accuracy')

plt.title('Training vs Validation Accuracy')
plt.xlabel('Epoch')
plt.ylabel('Accuracy')
plt.legend()

plt.show()
```

---

# New Concepts Learned

## 1. Multiple Convolution Layers

Instead of using a single convolution layer, two convolution layers were stacked.

Benefits:

* Learns more detailed image features
* Better feature extraction
* Higher classification accuracy

---

## 2. Dropout Regularization

Dropout randomly deactivates neurons during training.

```python
Dropout(0.5)
```

Meaning:

* 50% of neurons are temporarily ignored during each training step.
* Prevents the model from memorizing training data.
* Reduces overfitting.

---

## 3. Deeper CNN Architecture

Architecture Used:

Input Image (28×28×1)
↓
Conv2D (32 Filters)
↓
MaxPooling
↓
Conv2D (64 Filters)
↓
MaxPooling
↓
Flatten
↓
Dense (128)
↓
Dropout (0.5)
↓
Dense (10)
↓
Softmax

---

# Comparison with Day 1

| Feature            | Day 1    | Day 2    |
| ------------------ | -------- | -------- |
| Conv Layers        | 1        | 2        |
| Pooling Layers     | 1        | 2        |
| Dropout            | No       | Yes      |
| Feature Extraction | Basic    | Improved |
| Generalization     | Moderate | Better   |

---

# Results

Observed Improvements:

* Faster feature learning
* Better validation accuracy
* Reduced overfitting
* Improved generalization on unseen data

Expected Accuracy Range:

* Day 1 Model: 89–92%
* Day 2 Model: 91–94%

---

# Learning Outcomes

Through this task, the following concepts were learned:

* Deep CNN architectures
* Multiple convolution layers
* Hierarchical feature extraction
* Overfitting and underfitting
* Dropout regularization
* Model optimization
* CNN performance improvement techniques

---

# Conclusion

The CNN architecture was successfully enhanced by introducing additional convolutional layers and dropout regularization. The deeper network demonstrated improved feature extraction capabilities and achieved better generalization performance. This experiment highlighted the importance of architecture design and regularization techniques in deep learning-based image classification systems.
